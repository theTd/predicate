name: predicate
main: com.mineclay.predicate.PredicatePlugin
version: ${version}
author: i@thetd.me
depend:
  - groovyrt
commands:
  predicate:
    permission: predicate
    usage: /<command> <player> <conditions> then <command-to-execute>
    description: /predicate theTd '普通'=='普通' && matchset('event') then cpdc add event kill 1
